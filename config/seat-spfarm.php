<?php

return [
    /*
    |--------------------------------------------------------------------------
    | SP Farming Plugin Configuration
    |--------------------------------------------------------------------------
    |
    | This configuration file is loaded by the SeatSpfarmServiceProvider. At
    | present the plugin does not expose any user‑settable configuration
    | options at the system level. All per‑user preferences are stored in
    | the database (see migrations) and managed via the Settings page.
    |
    */

    // The base URI prefix for plugin routes. You may customise this if
    // conflicts arise with other routes. Do not include a leading slash.
    'route_prefix' => 'spfarm',
];